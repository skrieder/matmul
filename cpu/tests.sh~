#! /bin/bash                                                                                                                                                                       
matrixSize=64
for i in {1..9}
do
    ./runMeScott 10 $matrixSize
    matrixSize=$(expr $matrixSize + $matrixSize)
done
